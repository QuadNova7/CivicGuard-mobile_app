name: civicguard_mobileapp
description: "CivicGuard Citizen Emergency App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  google_fonts: ^6.1.0
  go_router: ^13.2.0
  image_picker: ^1.2.3
  geolocator: ^14.0.3
  geocoding: ^5.0.0
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
